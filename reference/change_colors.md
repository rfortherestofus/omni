# Change main colors in CSS

Change main colors in CSS

## Usage

``` r
change_colors(file, background_color = NULL, primary_font_color = NULL)
```

## Arguments

- file:

  CSS file path

- background_color:

  Background color (optionnal)

- primary_font_color:

  Primary color (optionnal)
