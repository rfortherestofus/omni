# Apply labels to survey

Apply labels to survey

## Usage

``` r
qualtrics_apply_labels(survey_data, labels_data)
```

## Arguments

- survey_data:

  Output or subset of omni::qualtrics_get_survey()

- labels_data:

  Output of omni::qualtrics_get_labels()

## Value

survey data with columns names replaced by labels
