# Change main fonts in CSS

Change main fonts in CSS

## Usage

``` r
change_fonts(file, main_font = NULL, secondary_font = NULL)
```

## Arguments

- file:

  CSS file path

- main_font:

  New main font (optionnal)

- secondary_font:

  New secondary font (optionnal)
