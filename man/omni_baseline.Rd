% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omni_header.R
\name{omni_baseline}
\alias{omni_baseline}
\title{Omni baseline axis line (non-bar/column charts only)}
\usage{
omni_baseline(n, orientation = c("vertical", "horizontal"), at = 0, pad = 0.3)
}
\arguments{
\item{n}{Number of categories on the categorical axis.}

\item{orientation}{`"vertical"` line (horizontal charts) or `"horizontal"` (vertical charts).}

\item{at}{Baseline position on the value axis (usually 0).}

\item{pad}{Extension beyond the first/last category center.}
}
\value{
A single `annotate("segment", ...)` layer.
}
\description{
Draws the light navy baseline next to the category labels, spanning only the data rows
(not the full panel height). Omit on bar/column charts, where the bars already begin at
the labels.
}
\examples{
library(ggplot2)
ggplot(mtcars, aes(mpg, factor(cyl))) +
  geom_point() +
  omni_baseline(n = 3)
}
